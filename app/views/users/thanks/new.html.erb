<% provide(:title, "サンクスページ登録") %>
<% provide(:btn_text, "登録") %>

<div class="card">
  <div class="card-body">
    <%= form_with model: @thank, url: users_thanks_path, method: :post, local: true do |f| %>
      <%= render partial: "form", locals: { f: f, thank: @thank } %>
    <% end %>
  </div>
</div>