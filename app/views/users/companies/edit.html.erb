<h2><% provide(:title, "企業メールアドレス設定") %></h2>

<div class="card">
  <div class="card-body">
    <p>お問い合わせ通知受信用のメールアドレスを編集できます。</p>
    <%= form_with model: @company, url: users_company_path, method: :patch do |f| %>
      <%= render 'shared/error_massages', object: @company %>

      <%= f.label :email %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %><br>

      <%= f.submit "更新", class: "btn btn-md btn-primary btn-block" %>
      <%= link_to "戻る", :back, class: "btn btn-md btn-block btn-default" %>
    <% end %>
  </div>
</div>
